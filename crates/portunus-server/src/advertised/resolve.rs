//! Tiered, SAN-filtered, fail-closed endpoint resolution.

/// Placeholder — full implementation lands in a later task.
///
/// # Errors
/// Never returns `Err` in this placeholder form.
#[allow(clippy::unnecessary_wraps)]
pub fn resolve_advertised_endpoint() -> Result<(), super::ResolveEndpointError> {
    Ok(())
}
