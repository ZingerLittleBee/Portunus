# Rust / Cargo
/target/
**/*.rs.bk
*.pdb

# Cargo.lock: commit for binaries, ignore for libraries.
# Default here assumes binary; remove the next line if this becomes a library crate.
# Cargo.lock

# IDE / editor
.idea/
.vscode/
*.swp

# Env
.env
.env.local

# Claude Code local-only files
.claude/settings.local.json
.claude/local/
.claude/.credentials*

# macOS
.DS_Store

# E2E test artifacts (bundles get re-issued on every run; not source of truth)
crates/forward-e2e/*.bundle.json
