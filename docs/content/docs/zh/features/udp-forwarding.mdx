---
title: UDP 转发
description: NAT 风格的 UDP 转发，每条源端流量自动获得一个内核分配的上游 socket。
icon: Send
---

v0.4.0 起支持。UDP 规则可以与 TCP 规则共用同一个监听端口 —— 内核会按协议
解复用。

## 工作原理

每条 UDP 规则为每个监听端口运行一个 listener 任务，每个任务持有一块固定大小
的接收缓冲。来自新终端 `(源地址, 源端口)` 的数据报会开启一条 **流**：代理绑定
一个新的上游 `UdpSocket`，一次性 `connect()` 到选定目标，并把该流登记到一个
按 `(监听端口, 源地址)` 索引的、规则级共享的 registry 中。回程数据报通过这个
registry 解复用回原始源。

接收缓冲的内存占用相对并发流数量是 **O(1)** —— 每个 listener 任务持有一块
固定大小的接收缓冲，不随流的数量增长，而不是每条流一块。空闲流由 idle
reaper 关闭（见下文 `udp_flow_idle_secs`）。

在 Linux 上，listener 使用批量系统调用（`recvmmsg`/`sendmmsg`，每次最多 32 个
数据报）来降低每包的系统调用开销。批量路径为每个 listener 持有一块固定的约
2 MiB 接收 arena（32 个槽 × 64 KiB），外加一块 64 KiB 的单包回退缓冲；两者都
不随流的数量增长。其它平台不使用批量 arena，走单块 64 KiB 的
`recv_from`/`send` 路径。

由于上游 socket 是 `connect()` 过的，Linux 会把 ICMP 错误
（`ECONNREFUSED`、`EHOSTUNREACH`、`ENETUNREACH`）反射回代理。受影响的流会被
立即驱逐；该源的下一个数据报会针对新选定的目标重建流。

## 推送 UDP 规则

```sh
portunus-server push-rule edge-01 6000 upstream.local:9999 --protocol udp
```

UDP 与 TCP 规则可以共用同一个监听端口：

```sh
portunus-server push-rule edge-01 6000 upstream.local:9999                # TCP
portunus-server push-rule edge-01 6000 upstream.local:9999 --protocol udp # UDP
```

## 可调参数

两项参数都在 `server.toml`，并通过 Welcome 消息下发到 Client：

```toml
# 流空闲多久后回收上游 socket。
# 范围 30..=300。默认 60。
udp_flow_idle_secs = 60

# 每条规则同时存活的 UDP 流上限。
# 范围 1..=65535。默认 1024。
udp_max_flows_per_rule = 1024
```

该上限是 **按规则计**，而非按端口计：一条端口范围规则若设
`udp_max_flows_per_rule = N`，则在其 **所有** 监听端口上合计最多接纳 `N` 条
并发流 —— 这是一个 registry 级的全局计数器，而不是 `N × 端口数`。

当大量不同源持续频繁地建立、销毁流而超过上限时，超出上限的流会让
`portunus_rule_flows_dropped_overflow_total` 累加。**已存在的流不会被驱逐
腾位** —— 稳态收缩由空闲回收来完成。

<Callout type="info">
  如果你的流量确实需要更多并发的终端源，请同时调高 systemd unit 中的
  `LimitNOFILE` 和 `udp_max_flows_per_rule`。
</Callout>

## 与端口范围 / DNS 联动

UDP 可与 [端口范围规则](/zh/docs/features/port-range)（每个端口一个 listener
任务，全部共享该规则的同一个流 registry 与按规则计的上限）和
[DNS 域名目标](/zh/docs/features/dns-targets)（解析器缓存与 single-flight
语义直接复用）一起使用。

## 指标

```
portunus_rule_udp_datagrams_in_total{client,rule,owner}
portunus_rule_udp_datagrams_out_total{client,rule,owner}
portunus_rule_active_flows{client,rule,owner}
portunus_rule_flows_dropped_overflow_total{client,rule,owner}
```

`rule-stats` 会增加一个 `protocol` 字段以及上述 UDP 计数器。

## Capability gate

向 v0.3.0 或更早的 Client 推 UDP 规则会得到 `unsupported_protocol`
（HTTP 422 / 退出码 3）。请先升级 Client。

## 验证过的性能

- 在一台开发机 macOS 上，criterion 基准
  `udp_data_plane.single_flow_throughput` 显示约 **78,000 datagrams/s**。
- 1000 个并发源端口的隔离压力测试 `test_udp_us1_thousand_source_isolation`
  零误路由。
