---
title: Web UI
description: 嵌入到 Server 二进制中的单页 React 控制台。登录、管理规则、看实时统计、浏览审计日志。
icon: LayoutDashboard
---

v0.6.0 起支持。UI 通过 `rust-embed` 嵌入到 `portunus-server`，从现有的操作员
HTTP listener 的根路径 `/` 提供。**部署主机上不需要任何 Node 运行时。**

## 打开

`portunus-server` 已经把 SPA 打包好了。在浏览器里打开操作员 HTTP listener
的地址即可：

```
http://127.0.0.1:7080/
```

支持的浏览器：Chrome / Firefox / Safari / Edge —— 最新两个版本。

## 登录

v1.1.0 起，Web UI 使用本地用户 ID + 密码登录。首次启动时，如果还没有任何
启用的 `superadmin`，Server 会把 setup token 打到 stderr，并且在 SQLite
里只保存它的哈希。打开 Web UI，输入这个 setup token，然后创建第一个
`superadmin`。

- setup token 30 分钟后过期。
- 如果 onboarding 还没完成，每次 server 进程启动都会轮换 setup token，旧
  token 立即失效。
- Web 登录会创建 `HttpOnly` 的 `portunus_session` cookie。SPA 不会把登录密钥
  写入 `sessionStorage` 或 `localStorage`。
- 用户由管理员手动创建，可设置初始密码。不支持公开自助注册。

API Bearer Token 仍然存在，但它和 Web 登录是两套东西。CLI 和自动化使用 API
Token（`PORTUNUS_OPERATOR_TOKEN`）；浏览器使用密码登录。

## 你能做什么

![Web UI 仪表盘，显示已连接客户端与生效规则统计。](../../../../src/screenshots/webui-dashboard.png)

| 区块 | 说明 |
| --- | --- |
| **Dashboard** | 在线 Client 数、活跃规则数、关键指标 |
| **Users** | （超管）添加 / 列出用户；签发凭据；查看 Grant |
| **Credentials** | 当前用户自助旋转 |
| **Grants** | （超管）RBAC Grant 管理 |
| **Rules** | push / list / remove。逐规则实时统计走 SSE（5 秒节奏；SSE 被拦时回退到轮询） |
| **Clients** | 列出与检视已连接的 `portunus-client` 实例 |
| **Audit log** | （超管）按 outcome 过滤，cursor 翻页，导出 NDJSON |
| **Metrics** | （超管）RBAC 守门后的原始 `/metrics` |
| **Settings** | 主题与语言切换 |

## 逐规则实时统计

规则详情页订阅 `GET /v1/rules/{id}/stats/stream` —— 一个每 5 秒推送一个
`RuleStatsSnapshot` 的 SSE 通道。订阅成本是 `O(规则数)` 而非
`O(规则 × 订阅者)`。慢消费者会收到 `Lagged`，每条规则每分钟最多打印一次。

## 多目标规则

规则详情的 Targets 区块展示健康徽标（Healthy / Degraded / Failed）、
最近一次失败 / 成功的时间戳，以及在 SSE 节奏下更新的逐目标字节计数。

## 限流与 QoS

规则编辑器多出一节 "Quality of service"。Burst 覆写默认折叠在 "Advanced"
下。规则列表多出一列紧凑的 `Caps`。Client 详情页多出一个 `Owner quotas`
标签，用于管理每个所有者的信封。

![规则编辑器中的服务质量上限输入。](../../../../src/screenshots/webui-rule-qos.png)

![Client 详情页中的所有者配额标签。](../../../../src/screenshots/webui-owner-quotas.png)

## 主题与 i18n

- **主题**：light / dark / `prefers-color-scheme`。
- **语言**：English + 简体中文（在 Settings 中切换；跨刷新记忆）。

CI 中有覆盖率单元测试 —— 翻译键在两个语言包之间不一致就会失败。

![设置页中的主题与语言切换。](../../../../src/screenshots/webui-settings.png)

## 监听地址

操作员 HTTP listener 默认使用 loopback。容器或可信反向代理场景可以设置
`operator_http_listen = "0.0.0.0:7080"`，再显式发布端口。远程访问由操作员负责：

- 从工作站 **SSH 隧道** 到 `127.0.0.1:7080`，或
- 把 listener 放在带自有认证的 **反向代理** 后面。

## 防密钥泄漏

- Web 登录密钥不会写入浏览器存储。
- session cookie 使用 `HttpOnly`、`SameSite=Lax`，启用 TLS 时带 `Secure`。
- API Token 和临时密码只在一次性 reveal 对话框中显示，不会放进 URL。

## 密码恢复

普通用户没有公开的 "forgot password" 自助入口。由 `superadmin` 在用户详情页
重置密码。重置会撤销该用户的 Web 会话，并默认撤销 API Token；只有管理员明确
勾选保留时才会保留 API Token。

如果最后一个 `superadmin` 忘记密码，只能本地恢复：

```sh
portunus-server --data-dir /var/lib/portunus reset-password _superadmin --temporary
```

该命令只打印一次临时密码，并标记账户下次登录必须修改密码。没有远程后门重置
接口。

## 构建

SPA 位于 `webui/`。Release 流水线执行：

```sh
cd webui
pnpm install --frozen-lockfile
pnpm build      # vite build + size-limit 守门（gzip ≤ 500 KB）
cd ..
cargo build --release -p portunus-server
```

仅迭代后端时：

```sh
PORTUNUS_SKIP_WEBUI=1 cargo build -p portunus-server
# 编译出 stub UI，让 rust-embed 始终有东西可嵌入。
```

Release 流水线绝不设置该 env。
